# This is an example .env file
NEXT_PUBLIC_SUPABASE_URL=https://yoururl.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY="<TOKEN_HERE>"

# REQUIRED: Service role key for server-side operations that bypass RLS
# Get this from: Supabase Dashboard → Settings → API → service_role key
# Used by /api/events to write parsed course data from n8n webhook
SUPABASE_SERVICE_ROLE_KEY="<SERVICE_ROLE_KEY_HERE>"

# OPTIONAL: Transcript parsing webhook configuration
# Leave these commented out if you haven't set up n8n yet
# Uploads will work without these, but parsing won't happen automatically

# URL to trigger your n8n workflow for parsing transcript PDFs
# TRANSCRIPT_PARSING_WEBHOOK_URL=https://your-n8n-instance.com/webhook/parse-transcript

# Shared secret to validate webhook calls from n8n (prevents unauthorized access)
# Generate with: openssl rand -hex 32
# TRANSCRIPT_WEBHOOK_SECRET=your-secure-random-secret-here